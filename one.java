package nothing;

import java.util.Scanner;

public class one {
    public static void main(String[] args) {
        Scanner next = new Scanner(System.in);
        System.out.println(next.next());// used for just one word
        System.out.println(next.nextLine());// used for printing complete statement
    }
}
